@model MyConservation.Models.NatureDocument

@{
    ViewBag.Title = "Details";
    Layout = "~/Views/Layouts/_Default.cshtml";
}
<div class="col-sm-9 col-sm-offset-3 col-lg-10 col-lg-offset-2 main">
<h2 id="Acc"> Les Details d'un domaine</h2>

<fieldset>
    <legend>Domaine Formation</legend>
    <div class="display-label">
         <h3>Identifiant :</h3>
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.id)
    </div>
    <div class="display-label">
         <h3>Nom du domaine :</h3>
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.nature)
    </div>
</fieldset>
<p>
    @Html.ActionLink("Modifier", "Edit", new { id = Model.id }, new {@class="btn btn-info" }) |
    @Html.ActionLink("Retourne sur la liste", "Index")
</p>
</div>