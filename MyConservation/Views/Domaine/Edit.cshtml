@model MyConservation.Models.DomaineFormation

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/LayoutsClient/_Default.cshtml";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>DomaineFormation</legend>

        @Html.HiddenFor(model => model.id)

        <div class="editor-label">
            @Html.LabelFor(model => model.nomDomaine)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.nomDomaine)
            @Html.ValidationMessageFor(model => model.nomDomaine)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
