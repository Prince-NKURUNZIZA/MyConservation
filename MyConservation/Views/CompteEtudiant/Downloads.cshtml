@{
    ViewBag.Title = "Downloads";
    Layout = "~/Views/LayoutsEtudiant/_Default.cshtml";
}

<header id="head" class="secondary"></header>  
 
 <div class="container Acceuil" >
  
@model List<string>  
<h2 id="Acc">Consulter et Telecharger les documents publiées</h2>  
<table id="exemple" class="table table-striped table-bordered ">
    <thead>
    <tr>  
        <th>Titre du document</th>  
          
    </tr> 
        </thead>
    <tbody> 
    @for (var i = 0; i <= Model.Count - 1; i++)  
    {   
        <tr>  
            <td> <h5>@Model[i].ToString() </h5></td>  
            <td>@Html.ActionLink("Télècharge directement", "Download", new { ImageName = @Model[i].ToString() }) </td>  
            <td>@Html.ActionLink("View", "ViewPDF", new { target = "_blank" })</td>
        </tr>  
    }  
        </tbody>
</table>
</div>
   </div>
@section scripts{
    <script src="~/Scripts/DataTable.js"></script>
    
    }